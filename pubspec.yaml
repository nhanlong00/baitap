name: core
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.19.6 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  get: ^4.6.5
  dio: ^5.3.3
  google_fonts: ^4.0.4
  flutter_form_builder: ^8.0.0
  file_picker: ^5.3.1
  form_builder_file_picker: ^3.0.0
  shared_preferences: ^2.2.2
  motion_toast: ^2.7.9
  http: ^0.13.6
  toastification: ^0.0.3
  bot_toast: ^4.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

